# Automation Tool
```
requires an ip address type of target: local machine or webApp

does ping and nmap scan: saves the result in a file in a directory

asks for types of attacks you want to perfom: directory brute-forcing, manual, metasploit payload, web-scrapping

in Directory-brute forcing: attacks with gobuster

in manual mode: exits the program

in passwd dictionary attack: uses 

in hash function cracking: uses john the ripper

in metasploit payload: searches the best exploit and does the attack

web-scrapping: makes use of web-scrapper for static websites only

in social-engineering: uses campish for camera based attacks and seeker for device info and location, toolkit also

for wireless attack: uses aircrack-ng 

for webapp attack: uses burpsuite

```